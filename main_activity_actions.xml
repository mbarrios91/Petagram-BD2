<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:yourapp="http://schemas.android.com/apk/res-auto">

    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_mascotas"
        android:title="@string/app_name"
        android:icon="@drawable/cat_footprint_50"
        yourapp:showAsAction="ifRoom"
        />
    <item android:id="@+id/action_star"
        android:icon="@drawable/starhotel_48"
        yourapp:showAsAction="ifRoom"
        android:title="star" />

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_contacto"
        android:title="@string/action_contacto"
        yourapp:showAsAction="never" />


    <item android:id="@+id/action_acerca_de"
        android:title="@string/action_acerca"
        yourapp:showAsAction="never" />


</menu>